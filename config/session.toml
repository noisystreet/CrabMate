# 会话相关嵌入片段（与 `config/default_config.toml` 在 `load_config` 中顺序合并：本文件在主默认之后、**`config/context_inject.toml`** 之前）。
# 进程内 REPL/TUI 会话 UI 开关已移除（D2.2）；历史路径名 `.crabmate/tui_session.json` 仍由 `workspace_session::session_file_path` 提供（供 `save-session` / tool-replay）。
# `max_message_history` 等会话裁剪键在 **`config/default_config.toml`**（可由 `CM_MAX_MESSAGE_HISTORY` 覆盖）。

[agent]
