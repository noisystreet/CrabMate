[package]
name = "crabmate-benchmark"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "CrabMate benchmark subsystem: batch agent evaluation across SWE-bench, GAIA, HumanEval etc."

[dependencies]
regex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
