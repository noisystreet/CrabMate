[package]
name = "crabmate-tool-card"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Shared tool result card compact/detail formatting for CrabMate (server + WASM)"

[dependencies]
serde_json = { workspace = true }
