.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.TH CRABMATE 1  "crabmate 0.5.0-alpha.0" "CrabMate User Commands"
.SH NAME
crabmate \- 基于 OpenAI 兼容 chat/completions 的 Rust AI Agent；本仓以 `serve` 为执行权威。官方终端为 Client 仓 `crabmate\-tui`
.SH SYNOPSIS
\fBcrabmate\fR [\fB\-\-config\fR] [\fB\-\-workspace\fR] [\fB\-\-no\-tools\fR] [\fB\-\-llm\-context\-tokens\fR] [\fB\-\-log\fR] [\fB\-h\fR|\fB\-\-help\fR] [\fB\-V\fR|\fB\-\-version\fR] <\fIsubcommands\fR>
.SH DESCRIPTION
基于 OpenAI 兼容 chat/completions 的 Rust AI Agent；本仓以 `serve` 为执行权威。官方终端为 Client 仓 `crabmate\-tui`
.SH OPTIONS
.TP
\fB\-\-config\fR \fI<CONFIG>\fR
显式指定配置文件路径（覆盖默认的 cwd / XDG `config.toml` 搜索）
.TP
\fB\-\-workspace\fR \fI<WORKSPACE>\fR
启动时指定初始工作区路径（覆盖配置中的 run_command_working_dir，仅当前进程生效）
.TP
\fB\-\-no\-tools\fR
禁用所有工具调用，仅作为普通 Chat 使用
.TP
\fB\-\-llm\-context\-tokens\fR \fI<N>\fR
模型上下文窗口 token 上限（输入+输出），覆盖配置 `llm_context_tokens` / `CM_LLM_CONTEXT_TOKENS`；省略则使用配置文件；`0` 视为不覆盖
.TP
\fB\-\-log\fR \fI<FILE>\fR
将日志追加写入指定文件（与 `RUST_LOG` 配合）。未设置 `RUST_LOG` 时，指定本选项会启用默认 **info** 级别写入，并同时输出到 stderr
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
\fB\-V\fR, \fB\-\-version\fR
Print version
.SH SUBCOMMANDS
.TP
\fBserve\fR
启动 HTTP API（默认纯 API；可选 `\-\-with\-web` 挂载 UI；默认端口 8080）
.TP
\fBbench\fR
批量 benchmark 测评（JSONL）
.TP
\fBconfig\fR
配置与自检（如 dry\-run）
.TP
\fBdoctor\fR
一页本地诊断（Rust/npm/前端路径、白名单条数等；人读，脱敏；**不要**求 API_KEY）
.TP
crabmate\-web\-bearer(1)
设置/查询/清除 Web API Bearer（系统钥匙串；**不要**求 API_KEY）
.TP
\fBmodels\fR
列出兼容网关 `GET …/models` 的模型 id（`llm_http_auth_mode=bearer` 时需 API_KEY；部分网关无此端点）
.TP
\fBprobe\fR
探测 api_base 上 models 端点连通性与 HTTP 状态（`llm_http_auth_mode=bearer` 时需 API_KEY）
.TP
\fBsave-session\fR
从会话文件导出 JSON/Markdown 到工作区 `.crabmate/exports/`（与 Web 导出约定一致；**不要**求 API_KEY）
.TP
crabmate\-tool\-replay(1)
工具调用时间线导出与重放（fixture / 回归；**不要**求 `API_KEY`）
.TP
crabmate\-sse\-replay(1)
从 SSE 事件录制文件回放 AG\-UI 事件到 TurnLayout 投影（**不要**求 API_KEY）
.TP
crabmate\-mcp(1)
MCP stdio 客户端运维：列出本进程内已缓存会话（**不要**求 API_KEY）
.TP
crabmate\-plugin(1)
动态工具模板与校验（工作区 `plugins/*.json`）
.TP
crabmate\-workflow(1)
工作流作者层 YAML/Markdown 编译与校验（**不要**求 API_KEY）
.TP
crabmate\-e2e(1)
真实 LLM e2e 端到端测试（需 API_KEY）
.PP
Use \fBcrabmate \-\-help\fR or \fBcrabmate \fIsubcommand\fR \fB\-\-help\fR.
.SH EXTRA
官方对话请：`crabmate serve` + Client `crabmate\-tui` / 桌面 / WASM。同进程 `chat|repl|tui` 入口已移除（见 docs/design/client_shell_split.md）。运维子命令与 SSE：**docs/命令行与路由.md**、**docs/SSE协议.md**。
.SH VERSION
v0.5.0\-alpha.0
