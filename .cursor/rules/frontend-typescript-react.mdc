---
description: 修改 frontend-leptos 时跑 wasm 检查与构建；与后端 API/SSE 契约对齐
globs: frontend-leptos/**/*
alwaysApply: false
---

# 前端（Leptos + WASM）

## 自检命令（仓库根目录）

- wasm 检查：`cd frontend-leptos && cargo check --target wasm32-unknown-unknown`
- 与生产一致构建：`cd frontend-leptos && trunk build`

大改组件、SSE 分发或打包配置后至少执行其一；准备合并或发版前建议执行 **`trunk build`**。

## 与后端一致

- 变更 **`POST /chat`**、**`/chat/stream`**、SSE `data:` JSON 形状、错误码时：同步 **`frontend-leptos/src/sse_dispatch.rs`** / **`api.rs`**（及消费 SSE 的组件）。若改动控制分发分支顺序：同步 **`fixtures/sse_control_golden.jsonl`**、**`src/sse/control_dispatch_mirror.rs`**，并跑 **`cargo test golden_sse_control`**。
- 新增/改名用户可见文案或行为时，按 **`.cursor/rules/todolist-and-documentation.mdc`** 评估是否更新 **`README.md`**。

## 其它

- 勿将真实 **API Key**、token 写入前端源码或提交到仓库；环境变量示例用占位符（见 **`.cursor/rules/secrets-and-logging.mdc`**）。
- 样式与结构尽量沿用现有 `frontend-leptos/styles.css` 与组件组织方式，避免无关大重构。
