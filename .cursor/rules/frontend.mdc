---
description: 业务 UI 已迁 Client 仓；本仓仅契约/文档指针
globs:
alwaysApply: false
---

# 前端（Leptos + WASM）

官方 UI 源码在 **`../crabmate-client/frontend`**（路径 A Phase 4.2）。

## 自检（Client 仓）

```bash
cd ../crabmate-client
make frontend-check    # wasm32 cargo check
make frontend          # trunk build
```

契约钉：`client-contract-v*`（见 Client `docs/design/contract_pin.md`）。

## 与后端一致

- 变更 **`POST /chat`**、**`/chat/stream`**、SSE 形状、错误码时：同步 Client `frontend/src/api/` 与本仓 `crabmate-sse-protocol` / fixtures。
- AG-UI classify 金样：`cargo test -p crabmate-sse-protocol golden_ag_ui_classify_matches_expected`；V2 parser 金样在 Client。
