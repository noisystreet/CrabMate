---
description: push 前先 rebase 上游主分支最新代码并解决冲突
alwaysApply: true
---

# Push 前：先同步并 Rebase 上游主分支

在代表用户执行 **`git push`** 前，必须先将当前工作分支基于上游主分支最新提交进行 rebase，并完成冲突解决，避免把“落后主线”的分支直接推上去。

## 标准流程

1. 确认当前分支与工作区状态：
   ```bash
   git status -sb
   git branch --show-current
   ```
2. 获取上游主分支最新代码（按仓库约定优先拉取指定分支）：
   ```bash
   git fetch origin main
   ```
3. 在当前功能分支执行：
   ```bash
   git rebase origin/main
   ```
4. 若出现冲突：
   - 手动解决冲突文件；
   - `git add <resolved-files>`；
   - `git rebase --continue`，直至 rebase 完成；
   - 若确认该提交不应保留，可 `git rebase --skip`（需在回复中说明原因）。
5. rebase 完成后再执行本仓库要求的检查（如 `pre-commit` / `cargo fmt` / `cargo clippy` / `cargo test` 等），最后再 push。

## 约束与例外

- **禁止**因图省事跳过 rebase 直接 push（除非用户明确要求保持 merge 流程且不 rebase）。
- 本规则不改变“默认不强推”的原则：若 rebase 造成非 fast-forward，需要 `--force-with-lease` 时，仍须有用户明确指令或会话中明确授权。
- 若上游主分支不是 `main`，按仓库分支策略替换为对应默认基线分支。
