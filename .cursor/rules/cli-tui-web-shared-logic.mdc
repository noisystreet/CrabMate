---
description: 非 UI 特有功能尽量在运维 CLI / Client 终端 / Web 提供；共享后端与契约
alwaysApply: true
---

# CLI / TUI / Web 逻辑复用

## 入口边界（D2.2 后）

本仓同进程 **`chat` / `repl` / `tui` 已硬删**。官方入口：

| 面 | 落点 |
|----|------|
| **Server / 运维 CLI** | 本仓 `serve`、`doctor`、`web-bearer`、`save-session`、`tool-replay` 等（`runtime/cli` 仅运维命令） |
| **终端对话** | Client **`crabmate-tui`**（HTTP + SSE） |
| **Web / Desktop** | Client UI；本仓 `lib` HTTP + SSE |

## 三端实现（默认期望）

**新增一项能力时**，先判断是否为**某一侧界面特有**（例如仅 Web 路由、仅 Client TUI 快捷键、仅运维子命令形态）：

- **若不是特有**：尽量在 **运维 CLI（本仓）**、**Client `crabmate-tui`**、**Web（Client UI + 本仓 API）** 中都能完成同类任务；差异只应是交互与展示。
- **若是特有**：可只实现该侧，但**数据模型、工具执行、配置键**仍应落在共享层（本仓 `agent/` / 契约 crate），便于扩展。

在此基础上避免各入口各写一套互不等价的业务规则。

## 优先方向

- **Rust 后端**：把「与 UI 无关」的规则放在可复用模块（如 **`agent/`**、**`types`**、**`sse::protocol`**、**`tool_registry`**、**`runtime/`** 下与 UI 无关的子模块），由运维 CLI、`lib` HTTP handler 与 Client 经契约薄封装调用。
- **会话与导出**：工作区会话文件、导出形状等已在 **`runtime/workspace_session`** 与 **`runtime/chat_export`**；同类能力扩展时延续同一契约。
- **HTTP 与 SSE**：Web 与 Client `crabmate-tui` 消费同一事件流，对齐 **`sse` 协议**与 **`run_agent_turn`** 编排。
- **前端**：展示与状态在 **Client `frontend/`**，但**请求体、SSE 事件、错误码**应与后端契约一致（见 **`api-sse-chat-protocol.mdc`**）。

## 不必强行共用的情形

- 纯 UI（键盘、焦点、组件）或终端绘制细节。
- 某一侧明确不需要该能力时，可只实现该侧，但**核心数据结构与工具执行**仍应走共享层。

## 与文档的关系

若复用关系导致**模块边界或调用链**变化，按 **`.cursor/rules/architecture-docs-sync.mdc`** 更新 **`docs/开发文档.md`**；若用户可见行为变化，按 **`.cursor/rules/todolist-and-documentation.mdc`** 更新 **`README.md`** 等。
