---
description: 修改 vendor/tui-markdown 时的边界（未来 TUI 复用）
globs: vendor/tui-markdown/**/*
alwaysApply: false
---

# vendored `tui-markdown`（`vendor/tui-markdown/`）

- **当前**主二进制**未**通过 `Cargo.toml` 依赖本目录；全屏 TUI 已从主路径移除后，vendor 仅作**后续再接终端 UI**时的参考/起点。
- **不要**提交或依赖 **`vendor/tui-markdown/target/`**；构建产物应留在本地并由 **`.gitignore`** 忽略。
- 若修复为通用改进，可考虑向 **上游仓库** 提 PR，再在 vendor 中同步版本，减少与主仓的永久分叉。
- 若重新接入 TUI 并恢复 `path` 依赖，须同步 **`docs/DEVELOPMENT.md`**（见 **`.cursor/rules/architecture-docs-sync.mdc`**）。
