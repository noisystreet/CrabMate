---
description: 禁止将 API key、.env 写入代码与提交说明；日志与回显须脱敏
alwaysApply: true
---

# 密钥与日志脱敏

## 禁止进入仓库与提交元数据

- **不得**将真实 **API key**、**token**、**refresh token**、私钥、数据库口令等写入：`src/`、`frontend/`、**`config/default_config.toml` / `config/session.toml` / `config/context_inject.toml` / `config/tools.toml` / `config/sandbox.toml` / `config/planning.toml` / `config/memory.toml` / `config.toml.example`**、测试夹具、脚本、**commit message**、PR/Issue 正文、或注释里的「看起来像真值」的示例。
- **`.env` / `.env.*`**：只放本地机（仓库已 `.gitignore`）；**不得**把文件**内容**贴进对话、文档、补丁或截图。文档与示例统一用占位符（如 `YOUR_DEEPSEEK_API_KEY`；若写 `sk-` 样例须标明**虚构**）。
- 若曾误提交真实密钥：**立即轮换**该密钥，并按需用历史重写工具清理（仅 revert 往往不够）。

## 日志与可观测性

- **`tracing` / `log` / `eprintln!` / 返回给前端的错误串**：**不得**输出完整密钥、整条 `Authorization: Bearer …`、或带真实 `?key=` / `?token=` 的 URL。
- 排错需要区分配置时：只记 **是否已设置**、**长度**、**前后各少量字符**（或项目内统一的 redact 辅助函数），避免可反推的值。
- 工具入参、子进程 stdout、第三方 HTTP 响应体若可能含凭证，写入磁盘或日志前做 **截断/替换**；面向用户的文案用泛泛描述（如「API 密钥无效」）。

## Agent 生成代码与回复时

- 补丁、示例命令、`curl`、配置片段中**默认使用占位符**；从用户环境读到的真实值**不要写回**仓库文件或公开回复。
- 审查 diff 时顺带检查：有无新增「长得像密钥」的常量、有无把 `process.env` / `std::env` 取值直接拼进日志。
