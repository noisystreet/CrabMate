---
description: 修改聊天 HTTP/SSE 协议时保持后端、前端与文档一致
alwaysApply: true
---

# 聊天与 SSE 协议（双端一致）

以下路径或概念变更时，**必须**同时核对 **Rust 路由/handler**、**前端解析** 与（若对外）**文档**：

- **路由与请求体**：`src/lib.rs` 中 **`/chat`**、**`/chat/stream`** 等；请求/响应 JSON 字段。
- **SSE 行协议**：`src/sse/protocol.rs`（及 `sse/mod.rs` 再导出）；流式 payload 类型、错误码、`data:` 行格式。
- **TUI 侧解析**：`src/sse/line.rs`（`classify_agent_sse_line` 等；常用符号亦经 `crate::sse::…`）是否与 Web 消费语义一致或刻意分叉（若分叉须在注释与 DEVELOPMENT 中说明）。
- **前端**：**`frontend/src/api.ts`** 及订阅流式结果的组件（错误分支、`tool_*` 等事件）。

## 清单

- [ ] 字段增删改名 → **serde 类型**、**前端 TypeScript**、**可选 README / DEVELOPMENT** 协议说明一致。
- [ ] 新错误码或 `code:` → 前端是否展示/重试；**勿**在响应体中泄露密钥或完整鉴权头（见 **`.cursor/rules/secrets-and-logging.mdc`**）。
- [ ] 准备提交 → **`cd frontend && npx tsc -b --noEmit`**；大改时 **`npm run build`**（见 **`frontend-typescript-react.mdc`**）。
