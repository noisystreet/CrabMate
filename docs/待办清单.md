**语言 / Languages:** 中文（本页）· [English](en/TODOLIST.md)

# 后续修复与完善清单

本文仅保留**未完成**待办。**某项完成后必须从本文件删除该条目**（勿长期保留 `[x]`）；空的小节可删掉标题。追溯完成时间请查 Git。维护约定见 **`.cursor/rules/todolist-and-documentation.mdc`**。

**结构**：

- **全局优先级（跨模块）**：按 **P0–P5** 共识项（安全、架构、测试、运维等）；与模块章可能交叉，实现后删除即可。
- **按模块分章**：每个功能域单独一章（`agent/`、`llm/`、`tools/` 等），章首**职责摘要**便于定位；条目前可含与全局段的交叉引用。

---

## 全局优先级（跨模块）

### P0 — 安全（非本机部署前建议处理）

（P0 安全项已落地：`openat2`/`mkdirat` 贯通写路径；`doctor` 输出 serve 部署检查；写工具审计默认开启。按用户账号鉴权仍须在网关/BFF，见 **`docs/未来规划功能.md`**。）

### P4 — 测试与质量

- [ ] **生产路径 unwrap/expect 审计**：梳理非测试代码中的 `unwrap` / `expect`（如 `per_coord`、`conversation_store`、命令退出码处理），改为显式传播或带业务上下文的 `expect`，降低低概率 panic 与排障成本。
- [ ] **集成/契约测试**：在 `lib_smoke` 与 **`tests/cli_contract.rs`**（`parse_args_from_argv`、`normalize_legacy_argv` fixture、`classify_model_error_message` / `EXIT_*`）之外，可为 `plan_artifact` 边界、`classify_agent_sse_line` 协议行、`workflow_reflection_controller` 状态迁移增加 fixture 或快照用例。
- [ ] **`stream_chat` 非流式**：可选 wiremock / 静态 JSON fixture 测 `ChatResponse` 解析。
- [ ] **Agent Benchmark 测评与基线**：在主流 agent benchmark（SWE-bench、HumanEval、GAIA 等）上对 CrabMate 做系统性评估，建立能力基线与回归对照，覆盖工具调用、多步推理、代码生成等；批量测评框架已具备（`--benchmark` + `--batch`，支持 SWE-bench / GAIA / HumanEval / Generic），后续在实际数据集上跑通完整流程并记录基线分数、持续追踪迭代。

### P5 — 运维与体验

- [ ] **跨进程 / 多副本队列**：当前为**单进程**内 `mpsc` + `Semaphore`；水平扩展需 Redis/SQS 等外部代理与持久化，本仓库未实现。
- [ ] **限流 / 配额**：对 `/chat`、`/chat/stream` 按 IP 或 token 限流（常与网关侧身份、配额同盘；进程内不做按用户账号，见 **`docs/未来规划功能.md`**）。
- [ ] **日志关联**：多轮会话落地后，可统一 `request_id` / `conversation_id`（依赖 P1 会话模型）；按用户审计宜在网关，见 **`docs/未来规划功能.md`**。

### 兼容层收缩（续）

> 切片计划见工作区 **`agent_space/compat-layer-shrink-plan.md`**（不入仓）。  
> **已完成**：**A3/A1/A2**（`44b13fd2`）；**B1 / Turn §16 E1**（本分支：`stream_draining` → 落盘 → `conversation_saved` → 最后 `RUN_FINISHED`；前端双序；`terminal_order` 软能力）；**C1–C3**（密钥迁移读路径弱化；MCP `toml_legacy_imported`；语义侧检默认仅 JSON）。  
> **下一步**：**B2**。下列为需兼容窗口项。

- [ ] **B2（E2）**：服务端可选布局元数据 + 共享投影 golden。
- [ ] **B3（E3）**：hydration 双读（新会话元数据优先，无元数据走 legacy）。
- [ ] **B4（E4）**：收缩删除终态后业务帧兼容、same-revision 止血、legacy merge/dedupe（须满足设计文档删除条件）。

---

## 路线图参考（对标主流开源 Agent）

以下由「与 AutoGen / CrewAI / LangGraph / Open Interpreter 等对照」整理为**方向性**待办，可与上文章节交叉推进；实现后按惯例删除条目。

- [ ] **多 Agent 协作**：多角色实例、消息路由与监督式编排（对标 AutoGen / CrewAI / MetaGPT）；与当前单 `agent_turn`（session_mode → ReAct）、会话与配额模型如何共存须在设计与 `docs/开发文档.md` 中预案。
- [ ] **交互式代码执行与受控 REPL**：在 `run_command` 白名单与沙盒策略之上，评估解释执行、会话级依赖安装与输出校验（对标 Open Interpreter）；安全面与 `tool_approval`、Docker 沙盒文档对齐。
- [ ] **工作流编排产品化**：在已有 `workflow_execute`（DAG）等能力上，补齐更接近 LangGraph 的**默认入口**——状态机式配置、条件/循环的可读表达、运行态可视化与排障故事；主聊天回合仍为线性时，在文档中写清「对话流 vs 工作流」边界。**架构设计见 `docs/工作流编排架构.md`（DAG/FSM 边界）、`docs/工作流Markdown作者层设计.md`（Markdown 双路径作者层）与 `docs/规划执行验证架构.md`（规划—执行—验证闭环与 `plan_rewrite` 正交）；实现前以该文为共识并随代码迭代修订。**
- [ ] **工具与连接器生态**：在 MCP 与 `dev_tag` 分栈工具之外，系统化高频集成（DB、云 API、办公等）或维护「推荐连接器」清单与接入模板，降低重复造轮子（与 `tools/` 章 MCP 扩展条同向）。**细分维度（并行编排、沙箱、观测、人机协同、命令映射等）**见 **`docs/design/tool_calling_evolution.md`**，落地时与本条及 `tools/` 章待办合并拆解，避免重复描述需求。
- [ ] **短期会话与主题轨迹**：多轮主题一致性、轮次级摘要与注入策略（与 `context_window`、长期记忆「检索质量」条目协同），缩小与主流「时序记忆 + 压缩」体验的差距。
- [ ] **可观测与执行轨迹**：在 tracing 之上，为 Web/CLI 提供回合内工具时间线、失败重试与推理/思维过程的统一可视化（与 `sse/` 调试类事件、横切 `request_id` 同向）。
- [ ] **文档、示例与社区引导**：Cookbook、典型场景模板、第三方工具/MCP 贡献说明，便于对标主流社区的扩展路径。

---

## `agent/`（回合编排、上下文、PER、工作流）

**职责摘要**：`agent_turn` 主循环（session_mode + Act 句启发式 → ReAct）；`context_window` 裁剪/摘要；`reflection/plan_rewrite` 终答规划重写与历史扫描；`per_coord` / `plan_artifact` / `workflow_reflection_controller`；`workflow_tool_dispatch`。

### 编排层改进（`agent_turn` / PER / 与 `llm` 共享契约）

> **现状（对齐现实）**：`hierarchy/` 与分阶段 `staged/` 编排入口已移除；运行时为 **session_mode →（Act 句关键词启发式）→ ReAct 外循环**；L2 意图分类已退役。另保留终答 Gate 与工作流反思两条正交机。勿再开「全局单表 FSM」或恢复分层路径 unless 另开设计稿。

- [ ] **编排层可测与可观测（增量）**：
  - **严守分层**：对外模型调用仅经 `llm::complete_chat_retrying`；会话侧语义变换仅在 `message_pipeline` / `context_window`；失败语义分层为 `LlmCompleteError` → `RunAgentTurnError`（含 `sub_phase`）→ SSE `code`（见 `docs/开发文档.md`）。
  - **单一决策入口**：Act 句启发式与 `plan_rewrite` 边界见 **`docs/配置说明.md`**；路由决议以 **`assess_turn_routing`** / **`TurnRouteDecisionV1`** 为准（相位词汇真源 **`phase_vocabulary`**）。
  - **收敛入参**：已拆 **`RunLoopParams`** / **`RunAgentTurnParams`**；归属真源 **`docs/design/run_loop_state_ownership.md`**（含 **`OuterLoopReflectMemo`**）。
  - **可测性**：外循环相位 / 路由 / **workflow↔Gate**（`workflow_to_plan_requirement_golden`）金样已落地；继续补 LLM·工具失败 → SSE 映射（见 **`.github/workflows/code-complexity.yml`**）。
  - **可观测性**：阶段迁移在统一位置打结构化日志（与 `TracingChatTurn`、`job_id`、`sub_phase`、`outer_loop_step`、Chrome trace 对齐）。

- [ ] **规划器/执行器：独立模型与预算（可选）**：模型端点切换已有（`OuterLoopPlanCallModelRole`）；观测类 **`LlmCallBudgetClass`** 已预留。后续可为 planner / executor 提供独立温度、max_tokens 与子预算（**不**恢复 `logical_dual_agent` 行为 unless 产品决策）。
- [ ] **规划与反思策略可插拔**：在现有 `FinalPlanRequirementMode` 与已落地的 `final_plan_require_*` / `final_plan_semantic_check_*` 之上继续按场景细化；与可选「观众」侧向点评的边界见 **`docs/design/audience_critic_role.md`**。
- [ ] **观众角色（侧向点评）**：按 **`docs/design/audience_critic_role.md`**（现行锚点 **C/D**）实现可选、有界、脱敏的无工具侧向调用；默认 fail-open；挂现有 P/R，不开并行阶段机。
- [ ] **测试与回归基线**：对 `plan_artifact` 解析边界增加快照/fixture 测试（与 P4 可合并实现）。

- [ ] **长期记忆：外部向量库与多租户**：当前已支持会话级 SQLite + 可选本地 **fastembed** 检索；后续可接 **Qdrant** / **pgvector**、跨会话租户键；与对外多租户身份及网关隔离方案（见 **`docs/未来规划功能.md`**）强绑定及更细 `redact` 策略。
- [ ] **长期记忆：检索质量**：混合检索（如 SQLite **FTS5** 与向量分数加权）、多轮上下文或会话主题感知的 **query** 构造，降低仅依赖末条 `user` 的漂移与漏召。
- [ ] **长期记忆：条目生命周期与去重**：**TTL**、访问频率或显式 **pin**；语义分块与近重复合并/淘汰策略，避免 `max_entries` 仅按时间删旧导致有效事实被挤出。
- [ ] **长期记忆：写入与索引策略**：选择性索引（跳过过短轮次、纯工具调试等）、同步索引与失败重试/补偿可配置；索引耗时与错误在可观测面汇总。
- [ ] **长期记忆：运营与合规 API**：在 **对外多租户身份与网关隔离**（见 **`docs/未来规划功能.md`**）前提下，提供只读列表/按 scope 删除等管理接口（排障与「被遗忘权」类需求）；`/status` 可暴露条目规模、embed 降级次数等（不脱敏全文）。
- [ ] **长期记忆：Web 与文档体验**：侧栏或面板展示「本轮注入摘要」、单条忽略/置顶；在 UI/README 中区分**长期记忆**、**工作区备忘文件**与**项目画像**的职责边界。

## `llm/` 与 `http_client.rs`（模型请求、重试、流式解析）

**职责摘要**：`ChatRequest` 构造、`complete_chat_retrying`；`api` 中 SSE/JSON 解析；共享 `reqwest::Client` 连接池与超时。

- [ ] **LLM 上游指标（可选）**：按 HTTP 状态/可重试维度暴露计数或对接外部 metrics（当前错误日志已含 `http_status` / `retryable` 字段）。
- [ ] **Token / 费用预估（可选）**：调用前按消息粗算 token，与 `context_window` 预算联动（Web 底栏已部分落地，见 **`frontend/`**「上下文用量」条目）。
- [ ] **非流式与流式一致性测试**：为 `stream: false` 路径补充契约测试（与 P4 同向）。
- [ ] **连接与 TLS 可观测**：可选 debug 级别记录连接复用、首字节延迟（不含敏感 URL 全量）。

---

## `tools/` 与 `tool_registry.rs`（工具实现与分发策略）

**职责摘要**：表驱动 `ToolSpec`、`run_tool`；`tool_registry` 中 Workflow / 阻塞超时 / 搜索等策略。

- [ ] **工具调用演进清单**：按 **`docs/design/tool_calling_evolution.md`** 分阶段选型实现（对标 LangGraph / MCP 产品 / Cursor 类人机协同等维度）；与 **`MCP` 扩展**、**`危险操作分级`**、**沙盒**、**`tool-replay`**、横切 **`request_id`** 等条目合并拆解，避免同一需求多处漂移描述。
- [ ] **危险操作分级与确认（续）**：写盘类等工具若需审批或细粒度策略，扩展 [`tool_approval::SensitiveCapability`] 与配置项（当前 `run_command` / `http_fetch` / `http_request` / 工作流审批已统一经 **`tool_approval`**）。
- [ ] **新栈工具按需扩展**：在 `dev_tag` 体系下按需增加其它语言栈标签与最小工具集（保持白名单与路径安全）。Go 已有 `go_build`/`go_test`/`go_vet`/`go_mod_tidy`/`go_fmt_check`/`golangci_lint`；JVM 已有 `maven_*`/`gradle_*`；容器已有 `docker_*`/`podman_images`；Node.js 已有 `npm_install`/`npm_run`/`npx_run`/`tsc_check`。
- [ ] **MCP 扩展（续）**：~~客户端 Streamable HTTP~~（已支持 `url`/`headers`，HTTPS 或 loopback HTTP）；~~多 server Bearer UX / `last_error_kind` / CLI `transport=`~~；OAuth、延迟 schema、能力矩阵与审批仍待；可选将本 agent 以 **HTTP**（streamable）暴露并与 Web 鉴权策略对齐；stdio server 已提供 **`crabmate mcp serve`**（见 **`docs/命令行与路由.md`**）。

---

## `sse/`（协议与行分类）

**职责摘要**：`protocol` 编码控制面 JSON；`line` 供 Rust 侧行分类（与 `frontend/src/sse_dispatch/dispatch.rs` / `api.rs` 消费语义对齐）。

- [ ] **调试/运维事件**：不脱敏前提下可关闭的 `debug` 类 payload（阶段名、耗时等），仅开发模式启用。

---

## `lib.rs` 路由、`chat_job_queue.rs`、`web/`（HTTP 接入与工作区 API）

**职责摘要**：Axum `Router`、`AppState`；对话队列；`web/workspace`、`web/task` 等。

- [ ] **鉴权与多租户隔离（进程内）**：当前为共享密钥；**不在本进程内**做按用户账号体系，部署形态见 **`docs/未来规划功能.md`**。若未来需消费反向代理信任头或多枚服务级密钥映射，须与安全面设计同盘立项。
- [ ] **会话与消息 API**：`messages` 或 `conversation_id` + 存储，与 `run_agent_turn` 对齐（与 P1 同向）。
- [ ] **上传配额与清理策略**：`/upload` 大小、类型、保留时间、按用户或 IP 限额。

---

## `config/`（配置加载与 CLI）

**职责摘要**：嵌入/文件 TOML、环境变量、`cli` 参数合并为 `AgentConfig`。

- [ ] **热重载（可选）**：仅对安全子集（工具开关、日志级别等）支持 SIGHUP 或文件 watch。
- [ ] **多 profile**：`dev` / `prod` 预设（工具白名单、审批模式、`http_fetch` 前缀等）。
- [ ] **密钥外置**：与密钥管理（vault、文件权限）集成，文档化兼容路径。

---

## `runtime/`（CLI、会话与导出）

**职责摘要**：`cli`；`workspace_session`、`chat_export`、`message_display`、终端着色与无 SSE 回显。

- [ ] **（未来）全屏终端 UI**：若重新引入 TUI，需恢复消息列表滚动/缓存与 SSE 行分类消费路径。
- [ ] **CLI 历史与脚本**：可选持久化输入历史、从文件批量注入用户消息。
- [ ] **无障碍与终端兼容**：弱终端、配色盲、宽字符与剪贴板失败时的降级提示。

---

## `frontend/`（Web UI，Leptos CSR + WASM）

**职责摘要**：根入口 `frontend/src/lib.rs`；HTTP/SSE 与本地存储见 `api.rs`、`sse_dispatch/dispatch.rs`、`storage.rs`、`app_prefs.rs`；主界面 `app/mod.rs`（`chat_column` / `ChatTuiStreamView`、`chat_composer`、`sidebar_nav`、`side_column`、`workspace_panel`、各 `*_modal` 等）；会话与导出见 `session_ops.rs`、`session_export.rs`、`session_search.rs`；Markdown 与展示见 `markdown.rs`、`message_format/`；样式与打包见 `frontend/styles/*.css`、`index.html`、`Trunk.toml`。

- [ ] **浏览器侧多轮状态（续）**：可选**加密**本地缓存；与 P1 会话模型其它项同向。已实现：`ChatSession` 持久化 **`server_conversation_id` / `server_revision`**，流式回合结束后 **`session_hydrate_nonce`** 触发 **`GET /conversation/messages`** 水合；标签页内仍用 **`frontend/src/session_sync.rs`** 的 **`SessionSyncState`**。
- [ ] **Web 底栏上下文用量（续）**：已实现底栏「上下文」芯片、新会话 system 基线（`~` + 虚线进度条）、**`stream_ended` / `conversation_saved` 附带 `tiktoken_prompt_tokens`**、AG-UI **`conversation_saved.tiktokenPromptTokens` / `RUN_FINISHED.tiktokenPromptTokens`** 即时更新与水合实测。后续可完善：
    - **水合失败短提示**：已有 `conversation_id` 但长期无 tiktoken 时提示重试，而非静默 `—`。
  - **L5 动态 skills**：按用户消息选材，基线 token 仍可能低于真实首包（已在 `GET /status` 纳入 L6）。
  - **分项明细**：tooltip 或悬停展示 system / 历史消息 /（可选）工具定义 JSON 近似 token。
  - **基线补全**：`GET /status` 的 `tiktoken_new_session_baseline_by_agent_role` 已纳入 L3+L4 **system** 与首轮 L6 项目上下文 `user`（工作区有效路径；L5 仍随用户消息动态变化，见上）。
  - **双指标**：同区展示 `effective_context_char_budget` 与 token 上限对照。
- [ ] **国际化与可访问性**：已集中 **`frontend/src/i18n/`**（按域拆分子模块，设置内语言切换）与 **`a11y.rs`**（主要模态焦点 + Tab 陷阱、全局 Esc 关闭弹层）；**剩余**：其它 `app/*` 若有零星硬编码、全量 grep 复核、以及各 `*_modal` / `session_message_search` 等待办条目逐项收口。
- [ ] **语音交互（未来）**：浏览器侧麦克风采集、STT（可对接云端或本地引擎）、TTS 播放；与现有聊天/SSE 流衔接；权限、隐私与错误降级文案；若走后端代理需在 `web/` 增加路由并与网关身份策略同盘（见 **`docs/未来规划功能.md`**）。

---

## 横切（`types`、`tool_result`、`health`、`redact`、`text_sanitize`）

**职责摘要**：OpenAI 兼容类型；工具结构化结果；`/health`；日志脱敏；展示层清洗。

- [ ] **请求关联 ID（剩余）**：HTTP 已有响应头 **`x-request-id`**（及部分 **`ApiError.request_id`**）；待贯穿 **tracing span** 与 **SSE** 帧（与 P5「日志关联」同向）。
- [ ] **健康与容量维度扩展**：在现有 **`GET /health`**（含可选 **`llm_models_endpoint`** / **`health_llm_models_probe`**）基础上，可选补充磁盘、队列深度等（实现时可合并为少数指标项）。
- [ ] **敏感信息规则库**：`redact` 与工具输出截断策略集中维护，新增工具时 checklist。

---

*说明：已完成工作不再写入本文件；必要时查 Git 提交记录。调整 `src/` 模块边界时同步更新 `docs/开发文档.md`（见 `.cursor/rules/architecture-docs-sync.mdc`）。安全敏感面见 `.cursor/rules/security-sensitive-surface.mdc`。*
